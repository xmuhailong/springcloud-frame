server:
  port: 2581

eureka:
  instance:
    hostname: eureka-server01
  client:
    # 注册中心不需要注册自己
    #register-with-eureka: false
    # 注册中心不需要去发现服务
    #fetch-registry: false
    service-url:
      defaultZone: http://eureka-server02:2582/eureka